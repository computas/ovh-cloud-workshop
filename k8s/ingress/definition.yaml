apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-app
  namespace: app
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  ingressClassName: traefik
  rules:
    - host: test.arsalankhalid.com
      http:
        paths:
          - backend:
              service:
                name: my-app
                port:
                  number: 80
            path: /frontend
            pathType: Prefix
  tls:
    - hosts:
        - test.arsalankhalid.com
      secretName: my-app-tls
